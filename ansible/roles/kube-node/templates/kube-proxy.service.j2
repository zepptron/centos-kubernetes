[Unit]
Description=Kubernetes Kube Proxy
Documentation=https://github.com/kubernetes/kubernetes

[Service]
ExecStart=/bin/kube-proxy --config={{ kube_proxy_dir }}/kube-proxy.yml
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target